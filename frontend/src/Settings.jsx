import React from "react";

function Settings() {
  return (
    <div style={{ padding: "40px", fontFamily: "Segoe UI, sans-serif" }}>
      <h2>Settings</h2>
      <p>This page will let the user change preferences like notifications, privacy, etc.</p>
    </div>
  );
}

export default Settings;
